{
  "name": "experium-modules",
  "main": "./index.js",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-react-app": "^2.2.0",
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "classnames": "^2.2.5",
    "ramda": "^0.23.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.3",
    "react-widgets": "^3.4.6",
    "redux": "^3.6.0",
    "redux-form": "^6.5.0",
    "redux-injector": "^0.1.0",
    "redux-saga": "^0.14.3",
    "redux-validate": "^0.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "compile": "NODE_ENV=production babel index.es6 --out-file index.js --presets=babel-preset-react-app",
    "compile-src": "NODE_ENV=production babel src --out-dir lib --copy-files --presets=babel-preset-react-app --plugins transform-regenerator",
    "postinstall": "npm run compile & npm run compile-src"
  }
}
